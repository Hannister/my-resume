export interface Icon {
  name: string;
  src: string;
  index: number;
  visible: boolean;

  // constructor(name: string, scr: string, index: number, visible: boolean) {
  //   this.name = name;
  //   this.src = scr;
  //   this.index = index;
  //   this.visible = visible;
  // }
}
